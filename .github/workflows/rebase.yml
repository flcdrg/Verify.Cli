name: Rebase
on:
  schedule:
    - cron: "0 0 * * *"
jobs:
  rebase:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/rebase@v3
